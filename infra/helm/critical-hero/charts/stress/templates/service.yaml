apiVersion: v1
kind: Service
metadata:
  name: stress
  namespace: {{ .Release.Namespace }}
spec:
  type: ClusterIP
  selector:
    app: stress
  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.port }}
