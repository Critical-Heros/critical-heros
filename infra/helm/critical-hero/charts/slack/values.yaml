# Defaults; overridden by the parent chart's `slack:` and `global:` sections.
replicaCount: 1

image:
  repository: ""
  tag: latest
  pullPolicy: Always

resources:
  requests:
    cpu: 100m
    memory: 128Mi
  limits:
    cpu: 250m
    memory: 256Mi
